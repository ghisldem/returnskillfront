import { AddSearchBarDirective } from './add-search-bar.directive';

describe('AddSearchBarDirective', () => {
  it('should create an instance', () => {
    const directive = new AddSearchBarDirective();
    expect(directive).toBeTruthy();
  });
});
