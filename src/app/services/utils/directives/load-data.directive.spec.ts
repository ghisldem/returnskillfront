import { LoadDataDirective } from './load-data.directive';

describe('LoadDataDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadDataDirective();
    expect(directive).toBeTruthy();
  });
});
